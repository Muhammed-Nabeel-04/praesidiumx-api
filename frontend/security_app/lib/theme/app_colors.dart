import 'package:flutter/material.dart';

class AppColors {
  // ─── Backgrounds ─────────────────────────────
  static const bg = Color(0xFF060B14);
  static const surface = Color(0xFF0D1521);
  static const surfaceHigh = Color(0xFF152030);
  static const appBar = Color(0xFF080E18);
  static const cardBorder = Color(0xFF1A2A40);

  // ─── Accents ─────────────────────────────────
  static const primary = Color(0xFF00D4FF);
  static const primaryDim = Color(0xFF003D4D);

  static const success = Color(0xFF00E676);
  static const successDim = Color(0xFF003D1A);

  static const danger = Color(0xFFFF3D5A);
  static const dangerDim = Color(0xFF4D0011);

  static const warning = Color(0xFFFFAB00);
  static const warningDim = Color(0xFF4D3300);

  static const purple = Color(0xFFBB86FC);
  static const purpleDim = Color(0xFF2D0066);

  // ─── Text ────────────────────────────────────
  static const textPrimary = Color(0xFFE8F4FD);
  static const textSecond = Color(0xFF607D8B);
  static const textMuted = Color(0xFF2D4A5C);
}
